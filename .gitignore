# QLever data directories (indexes, dumps, logs). Rebuilt from scratch via
# the scripts documented in README.md.
/olympics-test/
/wikidata-truthy/

# Editor / OS noise
.DS_Store
*.swp

# .opencode internals (node_modules, package.json, etc.), keep all skills
.opencode/*
!.opencode/skills/
!.opencode/skills/**